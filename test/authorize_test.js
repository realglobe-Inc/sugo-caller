/**
 * Test case for authorize.
 * Runs with mocha.
 */
'use strict'

const authorize = require('../lib/authorizing/authorize.js')
const assert = require('assert')
const co = require('co')

describe('authorize', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Authorize', () => co(function * () {

  }))
})

/* global describe, before, after, it */
