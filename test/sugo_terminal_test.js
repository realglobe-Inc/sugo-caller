/**
 * Test case for sugoTerminal.
 * Runs with mocha.
 */
'use strict'

const sugoTerminal = require('../lib/sugo_terminal.js')
const assert = require('assert')
const co = require('co')

describe('sugo-terminal', () => {
  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Sugo terminal', () => co(function * () {

  }))
})

/* global describe, before, after, it */
