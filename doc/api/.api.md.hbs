# {{pkg.name}}@{{pkg.version}}

{{pkg.description}}

{{#each signature.functions}}
+ [{{name}}({{#each parameters}}{{name}}{{#if @last}}{{else}}, {{/if}}{{/each}})](#{{{spinalcase ../pkg.name}}}-function-{{spinalcase name}})
{{/each}}
{{#each signature.classes}}
+ {{name}}
{{#with constructor}}
  + [new {{../name}}()](#{{{spinalcase ../../pkg.name}}}-classes-{{spinalcase ../name}}-constructor)
{{/with}}
{{#each functions}}
  + [{{../instanceName}}.{{name}}({{#each parameters}}{{name}}{{#if @last}}{{else}}, {{/if}}{{/each}})](#{{{spinalcase ../../pkg.name}}}-classes-{{spinalcase ../name}}-{{name}})
{{/each}}
{{/each}}

{{#each signature.functions}}
<a name="{{{spinalcase ../pkg.name}}}-function-{{spinalcase name}}" />
## {{name}}({{#each parameters}}{{name}}{{#if @last}}{{else}}, {{/if}}{{/each}}){{#if returns.type}} -> `{{{returns.type}}}`{{/if}}

{{{description}}}
{{#if parameters}}

| Param | Type | Description |
| ----- | --- | -------- |
{{#each parameters}}
| {{name}} | {{type}} | {{{description}}} |
{{/each}}

{{/if}}
{{/each}}

{{#each signature.classes}}

## {{name}}

{{description}}{{constructor.description}}

{{#with constructor}}

<a name="{{{spinalcase ../../pkg.name}}}-classes-{{spinalcase ../name}}-constructor" />
### new {{../name}}()

Constructor of {{../name}} class

| Param | Type | Description |
| ----- | --- | -------- |
{{#each parameters}}
| {{name}} | {{type}} | {{{description}}} |
{{/each}}

{{/with}}
{{#each functions}}

<a name="{{{spinalcase ../../pkg.name}}}-classes-{{spinalcase ../name}}-{{name}}" />
### {{../name}}#{{name}}({{#each parameters}}{{name}}{{#if @last}}{{else}}, {{/if}}{{/each}}){{#if returns.type}} -> `{{{returns.type}}}`{{/if}}

{{{description}}}
{{#if parameters}}

| Param | Type | Description |
| ----- | --- | -------- |
{{#each parameters}}
| {{name}} | {{type}} | {{{description}}} |
{{/each}}

{{/if}}
{{/each}}
{{/each}}



